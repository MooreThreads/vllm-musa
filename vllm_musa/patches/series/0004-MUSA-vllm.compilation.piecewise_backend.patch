From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: musa <musa@local>
Date: Wed, 3 Jun 2026 11:47:35 +0000
Subject: [PATCH] MUSA: vllm.compilation.piecewise_backend

---
 vllm/compilation/piecewise_backend.py | 8 +++++++-
 1 file changed, 7 insertions(+), 1 deletion(-)

diff --git a/vllm/compilation/piecewise_backend.py b/vllm/compilation/piecewise_backend.py
index b647e0d858..d89472d135 100644
--- a/vllm/compilation/piecewise_backend.py
+++ b/vllm/compilation/piecewise_backend.py
@@ -6,6 +6,7 @@ import io
 import json
 import pickle
 from collections.abc import Callable
+from contextlib import nullcontext
 from pickle import Pickler
 from typing import Any
 
@@ -220,7 +221,12 @@ class PiecewiseBackend:
 
         def serialize(fn: Callable[..., Any]) -> bytes:
             assert hasattr(fn, "serialize"), "fn must have serialize method"
-            with torch._functorch.config.patch("bundled_autograd_cache", True):
+            functorch_cache_ctx = (
+                torch._functorch.config.patch("bundled_autograd_cache", True)
+                if hasattr(torch._functorch.config, "bundled_autograd_cache")
+                else nullcontext()
+            )
+            with functorch_cache_ctx:
                 entry = fn.serialize()
 
                 f = io.BytesIO()
