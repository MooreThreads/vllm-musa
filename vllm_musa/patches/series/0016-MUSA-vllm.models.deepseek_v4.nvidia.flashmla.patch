From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: musa <musa@local>
Date: Wed, 3 Jun 2026 11:47:35 +0000
Subject: [PATCH] MUSA: vllm.models.deepseek_v4.nvidia.flashmla

---
 vllm/models/deepseek_v4/nvidia/flashmla.py | 12 +++++++++++-
 1 file changed, 11 insertions(+), 1 deletion(-)

diff --git a/vllm/models/deepseek_v4/nvidia/flashmla.py b/vllm/models/deepseek_v4/nvidia/flashmla.py
index a6a24ae0cc..c84070f4f2 100644
--- a/vllm/models/deepseek_v4/nvidia/flashmla.py
+++ b/vllm/models/deepseek_v4/nvidia/flashmla.py
@@ -26,7 +26,7 @@ from vllm.v1.attention.backends.mla.sparse_swa import (
     DeepseekSparseSWABackend,
     DeepseekSparseSWAMetadataBuilder,
 )
-from vllm.v1.attention.ops.flashmla import (
+from vllm_musa.v1.attention.ops.flashmla import (
     flash_mla_sparse_fwd,
     flash_mla_with_kvcache,
 )
@@ -207,8 +207,18 @@ class DeepseekV4FlashMLAAttention(DeepseekV4Attention):
                 topk_indices = attn_metadata.c128a_global_decode_topk_indices
                 topk_lens = attn_metadata.c128a_decode_topk_lens
 
+        active_decode_tokens = q.shape[0]
+        if topk_indices is not None:
+            topk_indices = topk_indices[:active_decode_tokens]
+        if topk_lens is not None:
+            topk_lens = topk_lens[:active_decode_tokens]
+
         swa_indices = swa_metadata.decode_swa_indices
         swa_lens = swa_metadata.decode_swa_lens
+        if swa_indices is not None:
+            swa_indices = swa_indices[:active_decode_tokens]
+        if swa_lens is not None:
+            swa_lens = swa_lens[:active_decode_tokens]
 
         # We treat queries in the same seq as different queries
         # and later we only attend by generated indices.
