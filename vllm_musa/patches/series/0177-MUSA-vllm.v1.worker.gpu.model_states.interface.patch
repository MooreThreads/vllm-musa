From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: musa <musa@local>
Date: Thu, 24 Sep 2026 22:27:21 +0800
Subject: [PATCH] MUSA: vllm.v1.worker.gpu.model_states.interface

---
 vllm/v1/worker/gpu/model_states/interface.py | 5 +++++
 1 file changed, 5 insertions(+)

diff --git a/vllm/v1/worker/gpu/model_states/interface.py b/vllm/v1/worker/gpu/model_states/interface.py
index 980ec1d..c449411 100644
--- a/vllm/v1/worker/gpu/model_states/interface.py
+++ b/vllm/v1/worker/gpu/model_states/interface.py
@@ -232,3 +232,8 @@ class ModelState(ABC):
     num_new_sampled_tokens_per_step: int = 1
     """New tokens sampled on each decode step 
     (excluding accepted draft tokens, a.k.a num bonus tokens)."""
+
+    def num_draft_tokens_per_req(self, input_batch: Any) -> Any:
+        """Draft tokens each request in the batch carries into its next step,
+        as an int array, or None when every request carries the buffer width."""
+        return None
-- 
2.34.1

